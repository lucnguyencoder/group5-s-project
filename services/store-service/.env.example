# Database (shared with other services for now)
DB_HOST=db
DB_PORT=3306
DB_USER=user
DB_PASSWORD=password
DB_NAME=swp391_db

# Server
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=1h

# Cookie
COOKIE_SECRET=yami2

# CORS
CORS_ORIGIN=http://localhost

# Supabase (if needed for images)
SUPABASE_URL=
SUPABASE_KEY=
